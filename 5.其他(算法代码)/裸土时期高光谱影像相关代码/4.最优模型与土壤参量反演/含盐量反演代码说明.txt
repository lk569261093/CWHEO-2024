该脚本的主要目标是通过光谱特征预测土壤中的含盐量（含盐量(g/kg)），并利用堆叠回归模型对高光谱图像进行预测，最终将预测结果保存为ENVI格式的图像文件。整个过程包括数据读取、特征选择、数据预处理、模型训练与保存、预测、评估、结果保存以及高光谱图像的预测与保存。以下是逐步的详细说明：
有机质反演代码与之类似

1. 导入必要的库

        
python
复制代码
import pandas as pd
import numpy as np
from sklearn.feature_selection import mutual_info_regression
from sklearn.model_selection import train_test_split
from sklearn.preprocessing import StandardScaler
from sklearn.linear_model import Ridge
from sklearn.svm import SVR
from sklearn.ensemble import RandomForestRegressor, StackingRegressor
from sklearn.metrics import mean_squared_error, mean_absolute_error
from scipy.stats import linregress
import matplotlib.pyplot as plt
import joblib
import spectral
from spectral import envi
解释：
Pandas & NumPy： 用于数据处理和数值计算。
Scikit-learn (sklearn)： 提供特征选择、模型训练、数据分割、标准化、回归模型、堆叠回归器以及评估指标等功能。
Scipy： linregress 用于线性回归分析。
Matplotlib： 用于数据可视化。
Joblib： 用于模型的保存和加载。
Spectral Library (spectral)： 用于处理高光谱图像，尤其是ENVI格式的图像文件。
2. 读取Excel数据文件

        
python
复制代码
# 读取Excel文件
file_path = '2.含盐量光谱特征25.xlsx'
data = pd.read_excel(file_path)
解释：
从路径 '2.含盐量光谱特征25.xlsx' 读取Excel文件，并将其存储为Pandas的DataFrame data。该文件包含光谱特征和对应的土壤含盐量数据。
3. 提取特征与目标变量

        
python
复制代码
# 提取土壤养分数据和光谱数据
y = data['含盐量(g/kg)']
X = data.drop(columns=['含盐量(g/kg)'])
解释：
y： 目标变量，表示土壤含盐量（含盐量(g/kg)）。
X： 特征矩阵，包含所有其他列（假定为光谱特征），通过删除目标列得到。
4. 使用互信息法进行特征选择

        
python
复制代码
# 互信息法选择特征
mi = mutual_info_regression(X, y, random_state=42)
mi_series = pd.Series(mi, index=X.columns, name='互信息值')
mi_series.to_excel('互信息值.xlsx', index=True)
    
selected_features_mi = X.columns[mi > 0.2]
X_selected_mi = X[selected_features_mi]
解释：
互信息回归 (mutual_info_regression)： 测量 X 中每个特征与目标变量 y 之间的依赖性。设置 random_state=42 确保结果可重复。
存储互信息值： 创建一个Pandas Series mi_series，将互信息值与特征名对应，并保存为Excel文件 '互信息值.xlsx'，便于后续查看。
特征选择阈值： 选择互信息值大于 0.2 的特征，存储在 selected_features_mi 中。
X_selected_mi： 包含通过互信息法选择的特征的特征矩阵。
备注： 互信息能够捕捉特征与目标变量之间的非线性关系，适用于回归任务中的特征选择。

5. 输出参与建模的波段

        
python
复制代码
# 输出参与建模的波段
selected_features_mi_df = pd.DataFrame(selected_features_mi, columns=['波段'])
selected_features_mi_df.to_excel('参与建模的波段.xlsx', index=False)
解释：
创建DataFrame： 将选中的特征（波段）转换为一个新的DataFrame selected_features_mi_df，列名为 '波段'。
保存为Excel文件： 将此DataFrame保存为 '参与建模的波段.xlsx'，便于查看哪些波段被选中用于建模。
备注： 这样可以明确了解哪些波段信息对含盐量的预测最为重要。

6. 数据预处理（标准化）

        
python
复制代码
# 数据预处理
scaler_X = StandardScaler()
scaler_y = StandardScaler()
X_scaled_mi = scaler_X.fit_transform(X_selected_mi)
y_scaled = scaler_y.fit_transform(y.values.reshape(-1, 1)).flatten()
    
# 保存用于标准化的 scaler_y 对象
joblib.dump(scaler_y, 'scaler_y.pkl')
解释：
标准化： 将特征和目标变量进行标准化处理，移除均值并缩放到单位方差。
scaler_X： 用于标准化特征矩阵。
scaler_y： 用于标准化目标变量。
X_scaled_mi： 通过互信息法选择的特征矩阵进行标准化后的数据。
y_scaled： 标准化后的目标变量。使用 reshape(-1, 1) 将一维数组转换为二维数组，以符合 StandardScaler 的输入要求，然后通过 flatten() 转回一维数组。
保存标准化对象： 使用 joblib.dump 将 scaler_y 保存为 'scaler_y.pkl'，以便后续在新数据上进行逆标准化处理。
备注： 标准化对于一些对特征尺度敏感的模型（如岭回归、支持向量机）至关重要。

7. 分割数据集为训练集和测试集

        
python
复制代码
# 分割数据集
X_train_mi, X_test_mi, y_train, y_test = train_test_split(X_scaled_mi, y_scaled, test_size=0.2, random_state=42)
解释：
train_test_split： 将数据集分割为训练集和测试集。
test_size=0.2： 保留20%的数据用于测试，80%用于训练。
random_state=42： 确保分割的可重现性。
结果：
X_train_mi & X_test_mi： 训练集和测试集的特征数据，通过互信息法选择的特征。
y_train & y_test： 训练集和测试集的目标变量。
8. 定义并训练基础模型

        
python
复制代码
# 定义和训练基础模型
rf = RandomForestRegressor(max_depth=10, n_estimators=100, random_state=42)    
svr = SVR(C=40, epsilon=0.01)

rf.fit(X_train_mi, y_train)
svr.fit(X_train_mi, y_train)
解释：
随机森林回归器 (RandomForestRegressor)：
参数设置：
max_depth=10： 树的最大深度，防止过拟合。
n_estimators=100： 森林中树的数量。
random_state=42： 确保结果可重复。
训练模型： 使用训练集 X_train_mi 和 y_train 进行训练。
支持向量回归 (SVR)：
参数设置：
C=40： 惩罚参数，控制模型对误差的惩罚程度，值越大表示对误差的惩罚越强。
epsilon=0.01： 控制模型的精度，决定了在回归过程中允许的误差范围。
训练模型： 使用训练集 X_train_mi 和 y_train 进行训练。
备注：
这些基础模型将作为堆叠回归器（Stacking Regressor）的基学习器（base learners）。
参数值（如 max_depth=10、n_estimators=100、C=40、epsilon=0.01）可能是通过先前的实验或调优得到的最佳值。
9. 定义元模型

        
python
复制代码
# 定义元模型
meta_model = Ridge(alpha=6)
解释：
岭回归 (Ridge)： 作为堆叠回归器的元模型（meta model）。
参数设置：
alpha=6： 正则化强度，控制模型复杂度，防止过拟合。
备注： 元模型用于综合基础模型的预测结果，以提升整体回归性能。
10. 创建堆叠回归器

        
python
复制代码
# 创建堆叠回归器
stacking_regressor = StackingRegressor(
    estimators=[('rf', rf), ('svr', svr)],
    final_estimator=meta_model,
    cv=5
)
解释：
StackingRegressor： 创建一个堆叠回归器，将多个基础回归模型的预测结果作为输入，训练一个元模型进行最终预测。
estimators： 基础模型列表，这里包括：
'rf'： 随机森林回归器。
'svr'： 支持向量回归。
final_estimator： 元模型，这里使用岭回归。
cv=5： 在进行堆叠时使用5折交叉验证，确保基础模型的泛化能力。
备注： 堆叠回归器通过结合多个模型的优势，通常能够提升预测性能。

11. 训练堆叠回归器

        
python
复制代码
# 训练堆叠模型
stacking_regressor.fit(X_train_mi, y_train)
解释：
训练过程： 在训练集 X_train_mi 和 y_train 上训练堆叠回归器。这包括：
基础模型训练： 随机森林和SVR在不同的训练子集上进行训练。
元模型训练： 使用基础模型的交叉验证预测结果作为输入，训练岭回归模型。
12. 保存训练好的模型

        
python
复制代码
# 保存模型
joblib.dump(stacking_regressor, '环境监测含盐量最优模型.pkl')
解释：
使用 joblib.dump： 将训练好的堆叠回归模型保存为 '环境监测含盐量最优模型.pkl'。这便于后续在生产环境中加载和使用该模型进行预测，而无需重新训练。
13. 进行预测

        
python
复制代码
# 预测
y_train_pred_stacking = stacking_regressor.predict(X_train_mi)
y_test_pred_stacking = stacking_regressor.predict(X_test_mi)
解释：
训练集预测： 使用训练集 X_train_mi 进行预测，得到 y_train_pred_stacking。
测试集预测： 使用测试集 X_test_mi 进行预测，得到 y_test_pred_stacking。
备注： 这里的预测结果仍然处于标准化后的状态（因为目标变量 y 被标准化过）。

14. 评估模型

        
python
复制代码
# 评估模型
def evaluate_model(y_true, y_pred):
    slope, intercept, r_value, p_value, std_err = linregress(y_true, y_pred)
    r2 = r_value**2
    rmse = np.sqrt(mean_squared_error(y_true, y_pred))
    mae = mean_absolute_error(y_true, y_pred)
    return r2, rmse, mae
解释：
定义评估函数 evaluate_model： 计算R²、均方根误差（RMSE）和平均绝对误差（MAE）。
linregress： 进行线性回归分析，获取斜率、截距以及相关系数 r_value。
r2： 相关系数的平方，表示模型的解释能力。
rmse： 均方根误差，衡量预测值与实际值的偏差。
mae： 平均绝对误差，衡量预测值与实际值的绝对偏差。
15. 评估堆叠回归模型

        
python
复制代码
# 评估堆叠模型
train_r2_stacking, train_rmse_stacking, train_mae_stacking = evaluate_model(y_train, y_train_pred_stacking)
test_r2_stacking, test_rmse_stacking, test_mae_stacking = evaluate_model(y_test, y_test_pred_stacking)
    
print(f'Training R2 (Stacking): {train_r2_stacking:.4f}, RMSE: {train_rmse_stacking:.4f}, MAE: {train_mae_stacking:.4f}')
print(f'Testing R2 (Stacking): {test_r2_stacking:.4f}, RMSE: {test_rmse_stacking:.4f}, MAE: {test_mae_stacking:.4f}')
解释：
训练集评估： 计算并存储训练集的R²、RMSE和MAE。
测试集评估： 计算并存储测试集的R²、RMSE和MAE。
打印结果： 将训练集和测试集的评估指标打印出来，便于快速了解模型性能。
示例输出解释：

        
复制代码
Training R2 (Stacking): 0.XXXX, RMSE: 0.XXXX, MAE: 0.XXXX
Testing R2 (Stacking): 0.XXXX, RMSE: 0.XXXX, MAE: 0.XXXX
R²： 越接近1表示模型越好地解释了数据的变异性。
RMSE 和 MAE： 越小表示模型的预测误差越低。
16. 逆标准化处理

        
python
复制代码
# 逆标准化处理
y_train_inv = scaler_y.inverse_transform(y_train.reshape(-1, 1)).flatten()
y_test_inv = scaler_y.inverse_transform(y_test.reshape(-1, 1)).flatten()
y_train_pred_stacking_inv = scaler_y.inverse_transform(y_train_pred_stacking.reshape(-1, 1)).flatten()
y_test_pred_stacking_inv = scaler_y.inverse_transform(y_test_pred_stacking.reshape(-1, 1)).flatten()
解释：
逆标准化： 将标准化后的目标变量和预测结果转换回原始尺度。
y_train_inv & y_test_inv： 训练集和测试集的实际值逆标准化后的结果。
y_train_pred_stacking_inv & y_test_pred_stacking_inv： 训练集和测试集的预测值逆标准化后的结果。
步骤说明：
reshape(-1, 1)： 将一维数组转换为二维数组，以符合 inverse_transform 的输入要求。
flatten()： 将二维数组转换回一维数组。
备注： 逆标准化后的结果更加直观，便于与实际值进行比较和解释。

17. 保存结果到Excel文件

        
python
复制代码
# 保存结果到Excel文件
train_results = {
    'Actual Train': y_train_inv,
    'Predicted Train (Stacking)': y_train_pred_stacking_inv
}

train_results_df = pd.DataFrame(train_results)
train_results_df.to_excel('训练集实际值和预测值.xlsx', index=False)

test_results = {
    'Actual Test': y_test_inv,
    'Predicted Test (Stacking)': y_test_pred_stacking_inv
}

test_results_df = pd.DataFrame(test_results)
test_results_df.to_excel('测试集实际值和预测值.xlsx', index=False)
解释：
创建结果字典：
train_results： 包含训练集的实际值和模型预测值。
test_results： 包含测试集的实际值和模型预测值。
转换为DataFrame： 使用 pd.DataFrame 将字典转换为DataFrame格式。
保存为Excel文件：
训练集： 保存为 '训练集实际值和预测值.xlsx'，不包含索引。
测试集： 保存为 '测试集实际值和预测值.xlsx'，不包含索引。
备注： 这样可以方便地在Excel中查看和比较实际值与预测值。

18. 可视化结果

        
python
复制代码
# 可视化结果
plt.figure(figsize=(10, 5))
plt.plot(y_train_inv, label='Actual Train')
plt.plot(y_train_pred_stacking_inv, label='Predicted Train (Stacking)')
plt.legend()
plt.title('Training Set: Actual vs Predicted (Stacking)')
plt.show()

plt.figure(figsize=(10, 5))
plt.plot(y_test_inv, label='Actual Test')
plt.plot(y_test_pred_stacking_inv, label='Predicted Test (Stacking)')
plt.legend()
plt.title('Test Set: Actual vs Predicted (Stacking)')
plt.show()
解释：
训练集可视化：
创建一个尺寸为10x5英寸的图形。
绘制训练集的实际值 (Actual Train) 和预测值 (Predicted Train (Stacking))。
添加图例和标题。
显示图形。
测试集可视化：
类似地，绘制测试集的实际值和预测值。
添加图例和标题。
显示图形。
备注： 通过可视化可以直观地比较模型在训练集和测试集上的预测效果，观察预测值与实际值的匹配程度。

19. 读取高光谱图像

        
python
复制代码
# 读取高光谱图像
img = spectral.open_image('Imagedata_R.hdr')
img_data = img.load()
解释：
打开高光谱图像： 使用 spectral.open_image 打开ENVI格式的高光谱图像头文件 'Imagedata_R.hdr'，并将其存储为 img 对象。
加载图像数据： 使用 img.load() 加载图像数据，得到 img_data，这是一个包含高光谱数据的数组。
20. 将图像数据转换为二维数组

        
python
复制代码
# 将图像数据转换为二维数组
n_rows, n_cols, n_bands = img_data.shape
img_reshaped = img_data.reshape((n_rows * n_cols, n_bands))
解释：
获取图像维度： n_rows（行数）、n_cols（列数）、n_bands（波段数）。
重塑图像数据： 将三维的高光谱图像数据重塑为二维数组 img_reshaped，其中每一行代表一个像素，每一列代表一个波段。
备注： 这种格式便于后续对每个像素的光谱数据进行处理和预测。

21. 从头文件中获取波长信息

        
python
复制代码
# 从头文件中获取波长信息
wavelengths = np.array(img.bands.centers)
解释：
获取波长信息： 从ENVI图像头文件中提取每个波段的中心波长，存储为NumPy数组 wavelengths。
备注： 了解每个波段对应的波长有助于选择与建模时相同的波段。

22. 根据波长信息匹配建模时的波段

        
python
复制代码
# 根据波长信息匹配建模时的波段
band_names = ['B90', 'B73', 'B80', 'B110', 'B34', 'B27']
band_indices = [int(band[1:]) - 1 for band in band_names]
解释：
定义建模时使用的波段名称： 存储在列表 band_names 中，如 'B90'、'B73' 等。
转换为波段索引： 提取波段编号（如从 'B90' 提取 90），转换为零基索引（即减去1），存储在列表 band_indices 中。
示例：
'B90' 对应的索引为 89（因为 90 - 1 = 89）。
23. 找到与建模波段匹配的高光谱波段索引

        
python
复制代码
# 找到与建模波段匹配的高光谱波段索引
selected_indices = [np.argmin(np.abs(wavelengths - wavelengths[idx])) for idx in band_indices]
解释：
匹配波长： 对于每个建模波段的索引 idx，找到高光谱图像中波长最接近该波段波长的位置。
np.abs(wavelengths - wavelengths[idx])： 计算所有波段波长与当前波段波长的绝对差。
np.argmin(...)： 找到最小差值的索引，即最接近的波长索引。
结果： selected_indices 列表中存储与建模时使用的波段最匹配的高光谱图像波段索引。
备注： 这种方法确保选取的高光谱波段与建模时使用的波段在波长上尽可能匹配，以提高预测的准确性。

24. 输出参与高光谱预测模型运算的波段

        
python
复制代码
# 输出参与高光谱预测模型运算的波段
selected_bands = wavelengths[selected_indices]
selected_bands_df = pd.DataFrame(selected_bands, columns=['波段'])
selected_bands_df.to_excel('参与高光谱预测的波段.xlsx', index=False)
解释：
获取选定波段的波长： 使用 selected_indices 从 wavelengths 数组中提取对应的波长值，存储在 selected_bands 中。
创建DataFrame： 将选定波段的波长转换为一个新的DataFrame selected_bands_df，列名为 '波段'。
保存为Excel文件： 将此DataFrame保存为 '参与高光谱预测的波段.xlsx'，便于查看哪些波段将用于高光谱图像的预测。
备注： 这样可以明确了解哪些波段的信息将用于实际的高光谱图像预测。

25. 选择与训练数据相同的特征

        
python
复制代码
# 选择与训练数据相同的特征
img_selected_features = img_reshaped[:, selected_indices]
解释：
提取选定波段的数据： 从重塑后的图像数据 img_reshaped 中提取所选波段的数据，存储在 img_selected_features 中。
这里 : 表示选择所有像素，selected_indices 表示选择特定的波段。
备注： 确保图像数据的波段与训练模型时使用的波段一致，这对于准确预测至关重要。

26. 标记零值为无效

        
python
复制代码
# 标记零值为无效
valid_mask = np.all(img_selected_features != 0, axis=1)
img_valid_features = img_selected_features[valid_mask]
解释：
创建有效掩码 valid_mask：
img_selected_features != 0： 检查每个像素在所有选定波段上的值是否不为零。
np.all(..., axis=1)： 对每个像素（行）进行检查，只有当所有选定波段的值都不为零时，该像素才被认为是有效的。
筛选有效像素： 使用 valid_mask 从 img_selected_features 中筛选出有效的像素数据，存储在 img_valid_features 中。
备注： 标记零值为无效可能是因为这些像素在某些波段上没有有效数据，或表示无效/缺失数据。

27. 标准化图像数据

        
python
复制代码
# 标准化图像数据
img_scaled = scaler_X.transform(img_valid_features)
解释：
应用标准化： 使用之前训练好的 StandardScaler 对象 scaler_X 对有效像素的特征数据 img_valid_features 进行标准化处理，存储在 img_scaled 中。
备注： 确保图像数据的特征与训练模型时使用的数据在相同的尺度上，提高预测准确性。

28. 使用训练好的模型进行预测

        
python
复制代码
# 使用训练好的模型进行预测
#y_pred_scaled = stacking_regressor.predict(img_scaled)
y_pred_scaled = stacking_regressor.predict(img_scaled)
#y_pred_scaled = np.maximum(y_pred_scaled, 0)  # 确保预测值非负
解释：
进行预测： 使用训练好的堆叠回归模型 stacking_regressor 对标准化后的图像数据 img_scaled 进行预测，得到标准化后的预测值 y_pred_scaled。
注释部分：
np.maximum(y_pred_scaled, 0)： （被注释掉）这一行代码用于确保预测值非负，将所有负值截断为零。这在某些情境下可能有用，尤其是当预测变量不可能为负时。
备注： 由于目标变量被标准化，预测结果需要进行逆标准化才能恢复到原始尺度。

29. 逆标准化预测结果

        
python
复制代码
# 逆标准化预测结果
#y_pred = scaler_y.inverse_transform(y_pred_scaled.reshape(-1, 1)).flatten()
y_pred = scaler_y.inverse_transform(y_pred_scaled.reshape(-1, 1)).flatten()
#y_pred = np.maximum(y_pred, 0)  # 将负值截断为零
解释：
逆标准化： 将标准化后的预测结果 y_pred_scaled 转换回原始尺度，得到 y_pred。
reshape(-1, 1)： 将一维数组转换为二维数组，以符合 inverse_transform 的输入要求。
flatten()： 将二维数组转换回一维数组。
注释部分：
np.maximum(y_pred, 0)： （被注释掉）这一行代码用于确保预测值非负，将所有负值截断为零。
备注： 逆标准化后的预测结果更具实际意义，便于与实际值进行比较和解释。

30. 创建一个全零数组来存储预测结果

        
python
复制代码
# 创建一个全零数组来存储预测结果
y_pred_img = np.zeros((n_rows * n_cols,))
y_pred_img[valid_mask] = y_pred
解释：
创建全零数组 y_pred_img： 初始化一个与总像素数相同长度的一维数组，全部值为零。
填充预测值： 使用 valid_mask 将有效像素位置的预测值 y_pred 填充到 y_pred_img 对应的位置。
无效像素位置保持为零。
备注： 这样做是为了保持预测结果与原始图像的空间结构一致，未预测的像素保持为零。

31. 将预测结果转换为图像格式

        
python
复制代码
# 将预测结果转换为图像格式
y_pred_img = y_pred_img.reshape((n_rows, n_cols))
解释：
重塑预测结果： 将一维的预测结果数组 y_pred_img 重塑为二维数组，形状为 (n_rows, n_cols)，即与原始图像相同的空间分辨率。
备注： 这样可以将预测结果映射回原始图像的空间布局，便于后续的可视化和保存。

32. 更新头文件信息

        
python
复制代码
# 更新头文件信息
metadata = img.metadata.copy()
metadata['lines'] = n_rows
metadata['samples'] = n_cols
metadata['bands'] = 1  # 只有一个预测结果波段
metadata['data type'] = 4  # float32
metadata['interleave'] = 'bsq'
metadata['byte order'] = 0
解释：
复制原始图像的元数据： 使用 img.metadata.copy() 复制原始图像的头文件信息，存储在 metadata 中。
更新元数据字段：
'lines'： 图像的行数（高度）。
'samples'： 图像的列数（宽度）。
'bands'： 更新为 1，因为预测结果只有一个波段。
'data type'： 设置为 4，表示数据类型为 float32。
'interleave'： 设置为 'bsq'（Band Sequential），即数据按波段顺序排列。
'byte order'： 设置为 0，表示小端格式。
备注： 更新后的元数据适用于保存单波段的预测结果图像。

33. 保存预测结果为ENVI格式

        
python
复制代码
# 保存预测结果为ENVI格式
output_file = '含盐量_distribution'
envi.save_image(f'{output_file}.hdr', y_pred_img, metadata=metadata, force=True)
解释：
定义输出文件名： '含盐量_distribution'。
保存为ENVI图像： 使用 envi.save_image 将预测结果 y_pred_img 保存为ENVI格式的图像文件。
文件名： 波段头文件 '含盐量_distribution.hdr'。
数据： y_pred_img。
元数据： 更新后的 metadata。
force=True： 如果文件已存在，则覆盖保存。
备注： 生成的ENVI图像文件可以用于进一步的遥感分析或可视化。

34. 可视化预测结果

        
python
复制代码
# 可视化预测结果
plt.imshow(y_pred_img, cmap='viridis')
plt.colorbar(label='hanyanliang (g/kg)')
plt.title('hanyanliang')
plt.show()
解释：
创建图形： 使用 plt.imshow 显示预测结果图像 y_pred_img。
cmap='viridis'： 选择色彩映射方案为 Viridis，颜色从蓝色到黄色渐变，适合展示连续值。
添加色条： 使用 plt.colorbar 添加色条，标签为 'hanyanliang (g/kg)'，表示不同颜色对应的含盐量值。
添加标题： 图像标题为 'hanyanliang'。
显示图形： 使用 plt.show() 显示最终的预测结果图像。
备注： 通过可视化可以直观地观察预测的空间分布情况，分析模型在不同区域的预测效果。